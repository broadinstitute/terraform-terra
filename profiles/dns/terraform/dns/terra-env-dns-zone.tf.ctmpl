resource "google_dns_managed_zone" "dns_zone" {
  name        = "{{env "OWNER"}}-terra-dns"
  dns_name    = "{{env "OWNER"}}-terra-dns.broadinstitute.org."
  description = "{{env "OWNER"}}-terra-dns zone"
}
